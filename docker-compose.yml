services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: kvalify
      POSTGRES_PASSWORD: kvalify
      POSTGRES_DB: kvalify
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
